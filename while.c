#include <stdio.h>

//typedef struct
//{
//    char kata[50];
//    int x;
//    int y;
//} titik;
int main()
{

    // int penghitung;
    // int tabint[5];
    // int ketemu;
    // for (penghitung = 0; penghitung < 5; penghitung++)
    //{
    //     printf("masukkan angka\n");
    //     scanf("%d", &tabint[penghitung]);
    //     printf("\n");
    // }
    // ketemu = 0;
    // penghitung = 0;
    // while ((ketemu == 0) && (penghitung < 5))
    //{
    //     if (((tabint[penghitung]) % 2) == 1)
    //     {
    //         ketemu = 1;
    //     }
    //     else
    //     {
    //         penghitung = penghitung++;
    //     }
    // }
    // if (ketemu == 0)
    //{
    //     printf("tidak ada angka ganjil\n");
    // }
    // else
    //{
    //     printf("ada angka ganjil\n");
    // }

    // int n;
    // scanf("%d", &n);
    // int tabInt[n];
    // int penghitung;
    // for (penghitung = 0; penghitung < n; penghitung++)
    //{
    // printf("masukkan angka:\n");
    // scanf("%d", &tabInt[penghitung]);
    // printf("\n");
    //}
    // penghitung = 0;
    // int maks = 0;
    // while ((maks < 4) && (penghitung < n)) //(max < x) untuk berapa angka yang akan ditampilkan
    //{
    // if (tabInt[penghitung] % 2 == 0)
    //{
    // printf("%d\n", tabInt[penghitung]);
    // maks++;
    //}
    // penghitung = penghitung++;
    //}

    //int n, i, jx = 0, jy = 0;
    //scanf("%d, &n");
    //titik ikatantitik[n];
    //for (i = 0; i < n; i++)
    //{
    //    printf("p");
    //}
    return 0;
}